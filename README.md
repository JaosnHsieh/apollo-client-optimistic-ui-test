## start json-graphql-server for mocking data

`$ yarn`

`$ npm start`

## start react project

`$ cd./apollo-optimistic-test`

`$ yarn`

`$ npm start`

## checkout different commit for different stage of testing
